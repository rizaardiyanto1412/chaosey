{
  "name": "@wasd/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "start": "serve -s dist -l 3000",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wasd/shared": "0.1.0",
    "colyseus.js": "^0.16.6",
    "phaser": "^3.90.0",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.1.12",
    "vitest": "^3.2.4"
  }
}
